/** @type {import('tailwindcss').Config} */
export const content = ["./src/**/*.{html,js}"];
export const theme = {
  extend: {
    colors: {
      customColor: '#ebeff3', // Replace with your own hex code
      // Add more custom colors if needed
    },
  },
};
export const plugins = [];
